name: 'AtoM Dev'

services:
  adminer:
    image: adminer:5.4.2
    depends_on:
      db:
        condition: service_healthy
    environment:
      ADMINER_DEFAULT_SERVER: db
    ports:
      - '0.0.0.0:8081:8080'
    restart: unless-stopped
